面向对象：封装、继承、多态   python 完美支持

类：具有相同属性和操作的对象的抽象

对象：客观存在的事务，可以是真实也可以是虚拟的

class 

类的定义

class 类名(父类):   //类名
    变量名=0        //类内函数外定义变量 类的变量 static
    def __init__(self, 变量1, 变量2...):    //构造函数->实例化
        self.变量名1                        //self.变量名 实例属性 属于每个对象
        self.变量名2
    
    def 函数(self, 变量1, 变量2...):        //self this指针
        return (xxx)


变量：类属性 和实例属性
类的属性的访问：类名.变量名 实例对象属性的访问：对象名.变量名

方法和函数有区别：生存周期

方法的绑定
def setSpeed(self, s):
    self.speed = s
import types
car1.setSpeed = types.MethodType(setSpeed, Car)

class的保护机制
定义变量或方法的时候使用 _ __ __方法__
_变量或方法 保护对象 